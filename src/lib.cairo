pub mod contracts {
    pub mod Jobs;
    pub mod fee_manager; // 🔐 FeeManager contract for fee collection and treasury
}

pub mod base {
    pub mod types;
}

pub mod interfaces {
    pub mod IJobs;
}
